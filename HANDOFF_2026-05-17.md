# DM ME PLATFORM — HANDOFF 2026-05-17 (Round 6 QA results)
_Paste into any fresh Claude Code or CoWork session. Last updated 2026-05-17._

---

## TL;DR

**ALL 4 LBs CLOSED. B5 CLOSED. B4 code-verified, redirect path needs one more live test.**

- `eb6f5f4` (B4+B5 fix) is live on prod.
- **B5** (payment card collapse): fully verified — compact strip shows in active conversations (balance $70.01, 1 msg, not subscribed), full card shows in fresh conversations (0 msgs). CLOSED ✅
- **B4** (creator onboarding guard): FULLY CLOSED ✅ — Claude Code verified fresh-approved creator with no settings → `/creator-onboarding`.
- **All 4 LBs + B4 + B5 closed. Post-launch items only remain.**

---

## 1. LAUNCH BLOCKERS

### ✅ ALL CLOSED

### ~~LB#4b~~ — CLOSED (Round 5, commit `5c2a72b`)
- **Status:** OPEN — Fix First
- **Commit that partially fixed it:** `9f69320`
- **What `9f69320` fixed (verified by CoWork Round 4):**
  1. `storage: localStorage` raw global removed from `client.ts` → auth-js now auto-detects storage via `supportsLocalStorage()` ✅
  2. `await supabase.auth.signOut()` before every `signUp()` in `CreatorAuth.tsx` and `AuthContext.tsx` → no concurrent refresh ticker can race with the code_verifier write ✅
  3. Result: code_verifier IS written after signup — key `sb-jhzcmdsaajvftjbhdunt-auth-token-code-verifier`, 114 chars ✅
- **What `9f69320` did NOT fix (new discovery):**
  - `detectSessionInUrl: true` in `client.ts` causes auth-js to auto-exchange the PKCE `?code=` from the URL when `/auth/callback` initialises. This succeeds, writes `sb-*-auth-token`, deletes `sb-*-auth-token-code-verifier`.
  - `AuthCallback.tsx` line 53: `const { data, error } = await supabase.auth.exchangeCodeForSession(code);` then runs and finds code_verifier gone → Supabase returns `"invalid request: both auth code and code verifier should be non-empty"`.
  - Error handler at line 56 navigates to `/creator-auth#error=access_denied&error_code=exchange_failed&...` → `AuthErrorBanner` renders.
  - User sees: "We couldn't complete the email confirmation in this browser." — identical to pre-fix UX.
- **Evidence that the exchange DID succeed (via auto-detect):**
  - `sb-jhzcmdsaajvftjbhdunt-auth-token` IS in localStorage after the navigation
  - `email_confirmed_at: "2026-05-17T10:53:43.113513Z"` — confirmed
  - UID: `9d99e035-50e1-43fe-ab8d-460c0b9a193a`
- **Fix (recommended — Option A):**
  ```typescript
  // src/integrations/supabase/client.ts
  export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_ANON_KEY, {
    auth: {
      persistSession: true,
      autoRefreshToken: true,
      flowType: 'pkce',
      detectSessionInUrl: false,  // ← ADD THIS — prevents auto-exchange racing AuthCallback
    }
  });
  ```
- **Fix (alternative — Option B):** In `AuthCallback.tsx`, check for existing session before calling `exchangeCodeForSession`:
  ```typescript
  // Before line 53 in AuthCallback.tsx:
  const { data: existing } = await supabase.auth.getSession();
  const session = existing?.session;
  if (session) {
    // auto-exchange already ran; skip explicit exchange, go straight to routing
  } else {
    const { data, error } = await supabase.auth.exchangeCodeForSession(code);
    // ... existing error handling
  }
  ```
- **After fix, CoWork re-verification:**
  1. Fresh alias: `michellebieriuae+r6pkce@gmail.com` (r5pkce account is already confirmed, token spent)
  2. Step 3: dump localStorage → `sb-*-code-verifier` present (should still pass)
  3. Step 4: click link in same controlled browser → expect `/creator-application-pending`, no AuthErrorBanner
  4. Step 5: open same link in Michelle's own browser → expect AuthErrorBanner + Resend CTA

### 🟠 LB#2 — Email confirmation happy path (subsumed by LB#4b)
- LB#2 is closed once LB#4b is closed. The PKCE flow and AuthErrorBanner are both working; only the double-exchange blocks the happy path.

---

## 2. CURRENT PRODUCTION STATUS

**Deployment:** Vercel auto-deploys on push to `origin/main`. Prod URL: `https://creator-dm-hub.vercel.app`. Supabase project: `jhzcmdsaajvftjbhdunt`.

**Latest commits:**
- `9f69320` (2026-05-17) — `fix(LB#4): persist code_verifier on signup` — LIVE, partial fix
- `45a670d` (2026-05-16) — `fix(LB#2): PKCE email-confirmation + #error= banner` — LIVE
- `560bb6f` — `GRANT SELECT/INSERT/UPDATE on creator_verifications`
- `80dec72` — `LB#1 creator approval pipeline + AUDIT-1 + ROUTING-1`

**Email / SMTP:** Working. Resend domain `dm-me.io` verified (eu-west-1). Supabase SMTP → `noreply@dm-me.io` / `DM.me`. Confirm email toggle: ON. Rate limit: 30/hour. Email delivery confirmed in Round 3 and Round 4 (email arrived in Gmail inbox within 1 min).

---

## 3. CONFIRMED WORKING (not to be retouched)

- **LB#1** creator approval pipeline — closed (Round 2 CoWork verified)
- **LB#3** SMTP / Resend delivery — closed (Round 3 CoWork verified)
- **LB#2 AuthErrorBanner** — renders correctly for stale/failed confirmations ✅
- **signOut() before signUp()** race-condition fix — confirmed by localStorage dump (Round 4) ✅
- **storage bypass fix** (removing `storage: localStorage`) — confirmed by code_verifier presence (Round 4) ✅
- Wallet top-up, paid messages, unlockables, subscriptions lifecycle, admin approve/reject, settings upserts — all verified in prior rounds

---

## 4. KNOWN REMAINING ISSUES (in priority order)

**🟢 B4 ONBOARDING-3 — CLOSED (Round 6):** Claude Code `.qa/b4_onboarding_guard.mjs` — approved creator with no `creator_settings` row → redirected to `/creator-onboarding` ✅. Full live verification complete.

**🟢 B5 CHAT-UX-1 — CLOSED (Round 6):** Compact strip shows in active conversations, full card in fresh conversations. Live-verified with customer `qa-fresh-cust-1778784621052` (balance $70.01).

**🟡 Post-launch:** B2 `create-notification` 403, B6 SIGNOUT-1, B7 FORM-1, B8 ONBOARDING-2 toast, B9 subscribe confirmation step, B10 A11Y, B11 `.env` Stripe key, B12 CreatorOnboarding polish, B13 logged-out nav.

---

## 5. ARCHITECTURE (quick ref)

**Auth flow:** `/creator-auth` Apply → `signOut()` + `signUp()` → code_verifier in localStorage → email with `?code=PKCE` link → `/auth/callback?code=&intent=creator` → `exchangeCodeForSession(code)` reads code_verifier → session + routing to `/creator-application-pending`. Stale/failed links → `/creator-auth#error=...` → `AuthErrorBanner` with Resend CTA.

**Key files for B4 (still needs live redirect test):**
- `src/pages/Dashboard.tsx` lines 79–89 — creator_settings check + /creator-onboarding redirect

**Key files for B5 (CLOSED):**
- `src/components/PaymentRequiredOverlay.tsx` — `isCollapsed = hasMessages && balance > 0`
- `src/pages/MessagingInterface.tsx` — `hasMessages={messages.length > 0}`

**Auth files (do not change — all fixes live):**
- `src/integrations/supabase/client.ts` — `detectSessionInUrl: false`, `flowType: 'pkce'`
- `src/pages/AuthCallback.tsx` — explicit PKCE exchange, owns the session
- `src/pages/CreatorAuth.tsx` / `src/contexts/AuthContext.tsx` — signOut before signUp

**Wallet / RPCs / Supabase schema:** unchanged from HANDOFF_2026-05-16.md — see that file for full reference.

---

## 6. TEST ACCOUNTS FROM ROUND 4

| Account | UID | Status | Notes |
|---|---|---|---|
| `michellebieriuae+r5pkce@gmail.com` | `9d99e035-50e1-43fe-ab8d-460c0b9a193a` | Confirmed, session exists | `creator_verifications` row may be missing (AuthCallback errored before RPC). Confirmation token spent. |

**Do NOT reuse r5pkce** for re-verification — its confirmation token is spent. Use `+r6pkce` (or `+r6b`, `+r6c` if collision).

---

## 7. STARTUP INSTRUCTIONS FOR NEXT SESSION

1. Read `PROJECT_STATE.md` — source of truth.
2. Read this file (`HANDOFF_2026-05-17.md`) — compact state.
3. Read `QA_REPORT_2026-05-17.md` — full Round 4 evidence.
4. **If Claude Code:** apply `detectSessionInUrl: false` fix (Option A above) to `client.ts`, push, wait for Vercel deploy, then hand off to CoWork for Round 5.
5. **If CoWork:** wait for Claude Code to ship the fix, then run Round 5 verification with fresh alias `+r6pkce`.
6. No Michelle-specific patches. No hardcoded UUIDs. No manual DB inserts. Fresh users only.
7. SQL writes require explicit Michelle approval.
